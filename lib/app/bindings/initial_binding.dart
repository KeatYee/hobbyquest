import 'package:get/get.dart';
// import '../controllers/auth_controller.dart'; 

class InitialBinding extends Bindings {
  @override
  void dependencies() {
    // Example: Put the AuthController here so it's always ready
    // Get.put(AuthController(), permanent: true);
  }
}